{
  "name": "@onnx9000/checker-ui",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "lint": "oxlint -c $(git rev-parse --show-toplevel)/oxlintrc.json",
    "test": "vitest run --passWithNoTests --environment jsdom"
  },
  "dependencies": {
    "@onnx9000/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^4.0.0"
  },
  "type": "module"
}
