[project]
name = "onnx9000-onnx2tf"
version = "1.0.0"
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[tool.hatch.build.targets.wheel]
packages = ["src/onnx9000_onnx2tf"]
[tool.pytest.ini_options]
testpaths = ["tests"]