[project]
name = "onnx9000-ort-training"
version = "1.0.0"
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[tool.hatch.build.targets.wheel]
packages = ["src/onnx9000_ort_training"]
[tool.pytest.ini_options]
testpaths = ["tests"]