[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "onnx9000-progressive-loading"
version = "1.0.0"
description = "Progressive Loading for ONNX9000"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "onnx>=1.14.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/onnx9000_progressive_loading"]
