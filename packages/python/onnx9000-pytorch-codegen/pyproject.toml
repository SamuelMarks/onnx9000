[project]
name = "onnx9000-pytorch-codegen"
version = "1.0.0"
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[tool.hatch.build.targets.wheel]
packages = ["src/onnx9000_pytorch_codegen"]
[tool.pytest.ini_options]
testpaths = ["tests"]