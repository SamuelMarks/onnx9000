[project]
name = "onnx9000-mlir-lowering"
version = "1.0.0"
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[tool.hatch.build.targets.wheel]
packages = ["src/onnx9000_mlir_lowering"]
[tool.pytest.ini_options]
testpaths = ["tests"]