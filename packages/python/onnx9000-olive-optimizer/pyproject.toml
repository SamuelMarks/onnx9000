[project]
name = "onnx9000-olive-optimizer"
version = "1.0.0"
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[tool.hatch.build.targets.wheel]
packages = ["src/onnx9000_olive_optimizer"]
[tool.pytest.ini_options]
testpaths = ["tests"]