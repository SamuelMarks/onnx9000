[project]
name = "onnx9000-skl2onnx"
version = "1.0.0"
description = "SKL2ONNX Converter"
authors = [
    { name = "Samuel Marks", email = "samuel@example.com" }
]
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/onnx9000_skl2onnx"]

[tool.pytest.ini_options]
testpaths = ["tests"]
