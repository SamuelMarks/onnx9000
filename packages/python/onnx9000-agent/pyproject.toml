[project]
name = "onnx9000-agent"
version = "1.0.0"
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[tool.hatch.build.targets.wheel]
packages = ["src/onnx9000_agent"]
[tool.pytest.ini_options]
testpaths = ["tests"]