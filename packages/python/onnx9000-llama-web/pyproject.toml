[project]
name = "onnx9000-llama-web"
version = "1.0.0"
description = "LLaMA-Web SDK"
authors = [
    { name = "Samuel Marks", email = "samuel@example.com" }
]
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/onnx9000_llama_web"]

[tool.pytest.ini_options]
testpaths = ["tests"]
