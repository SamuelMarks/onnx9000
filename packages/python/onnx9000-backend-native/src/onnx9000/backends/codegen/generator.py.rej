@@ -115,7 +115,8 @@
 
         # Iterate over nodes and call op generators
         for node in self.graph.nodes:
-            op_gen = registry.get_generator(node.op_type, domain=node.domain)
+            domain = getattr(node, "domain", "")
+            op_gen = registry.get_generator(node.op_type, domain=domain)
             op_code = op_gen(node, self)
             body.append(op_code)
 
