/**
 * @fileoverview index.ts
 * Provides index functionality for the progressive-loading package.
 */
export class ProgressiveLoading {
  process(arg: string) {
    return `Progressive Loading processed ${arg}`;
  }
}
