/**
 * @fileoverview index.ts
 * Provides index functionality for the zero-dep-classifier package.
 */
export class ZeroDepClassifier {
  process(arg: string) {
    return 'Zero Dep Classifier processed ' + arg;
  }
}
