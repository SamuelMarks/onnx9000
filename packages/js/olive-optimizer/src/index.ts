/**
 * @fileoverview index.ts
 * Provides index functionality for the olive-optimizer package.
 */
export class OliveOptimizer {
  process(arg: string) {
    return `Olive Optimizer processed ${arg}`;
  }
}
