/**
 * @fileoverview index.ts
 * Provides index functionality for the triton-server package.
 */
export class TritonServer {
  process(arg: string) {
    return `Triton Server processed ${arg}`;
  }
}
