/**
 * @fileoverview index.ts
 * Provides index functionality for the new-model-arch package.
 */
export class NewModelArch {
  process(arg: string) {
    return `New Model Arch processed ${arg}`;
  }
}
